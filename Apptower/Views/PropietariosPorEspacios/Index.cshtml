@model IEnumerable<Apptower.Models.PropietariosPorEspacio>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEspacioNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPropietarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdEspacioNavigation.NombreEspacio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPropietarioNavigation.NombrePropietario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPropietariosPorEspacio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPropietariosPorEspacio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPropietariosPorEspacio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
